# What the server needs at run time. The Dockerfile installs only this.
fastapi>=0.115
uvicorn[standard]>=0.30
segno>=1.6                # QR for the join link
cryptography>=42          # seal.py — Fernet + scrypt, and the passphrase gate
