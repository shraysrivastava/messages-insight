# Everything above, plus what only ever runs on your laptop.
-r requirements.txt

# Authoring tools
pytypedstream>=0.1        # decodes attributedBody; without it we byte-walk

# Tests
pytest>=8
pytest-asyncio>=0.24      # main.py is a socket server; its tests are async
httpx>=0.27               # fastapi's TestClient rides on it
